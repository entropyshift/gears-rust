name: Clippy (full feature matrix)

# Full cargo-hack --each-feature run across the entire workspace.
# ~182 runs, ~30-40 min. Not suitable for PRs; runs nightly and on every
# push to main so regressions are caught before they accumulate.
#
# To manually trigger: Actions → Clippy (full feature matrix) → Run workflow.
on:
  schedule:
    - cron: '0 2 * * *'   # 02:00 UTC daily
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: clippy-nightly-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: full
  CARGO_INCREMENTAL: "0"

jobs:
  clippy-deep:
    name: Clippy deep (all features × all crates)
    runs-on: ubuntu-latest
    env:
      RUSTFLAGS: "-C debuginfo=0"
      # Must exist BEFORE the probe step's `sccache --start-server` — the
      # server binds its storage backend (GHA cache vs local disk) at startup.
      SCCACHE_GHA_ENABLED: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install protoc
        uses: taiki-e/install-action@9114bf4d891761788c546334fd37538eae1bf8b3 # v2.87.16
        with:
          tool: protoc@3.34.1

      - name: Install Go (required for aws-lc-fips-sys FIPS build)
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.21.x'
          cache: false

      - name: Install Rust toolchain from rust-toolchain.toml
        run: |
          rustup show active-toolchain || rustup toolchain install
          rustup show

      - name: Install cargo-hack
        uses: taiki-e/install-action@9114bf4d891761788c546334fd37538eae1bf8b3 # v2.87.16
        with:
          tool: cargo-hack

      - name: Cache Cargo/target
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          cache-targets: false
          shared-key: nightly-clippy-${{ hashFiles('rust-toolchain.toml') }}

      - name: Install sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba # v0.0.11

      - name: Probe sccache & set RUSTC_WRAPPER
        shell: bash
        run: |
          # SCCACHE_GHA_ENABLED is set as job-level env, so the server starts
          # on the GHA cache backend (the backend is fixed at server startup).
          if sccache --start-server > /tmp/sccache-start.log 2>&1; then
            echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"
          else
            echo "sccache failed, falling back to plain rustc; startup output:"
            cat /tmp/sccache-start.log || true
          fi

      - name: cargo clippy (full feature matrix)
        run: make clippy-deep
